Nome do projeto - Sistema-aluno;

O que faz? Mostra as informações do aluno como nome, idade e se reprovou.

Nome do aluno? Adenilson Santos;

Como rodar? O código tem as variáveis "nome", "idade" e "reprovou". Você precisa inserir os dados necessários para cada variável e, rodando o código, o console.log() vai mostrar as informações do aluno
